{# templates/song/updateSong.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Song</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Update</button>
    {{ form_end(form) }}
{% endblock %}
